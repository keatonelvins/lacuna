# uv run --no-project --with mergekit merge/nearswap.yaml --gpu-rich

merge_method: nearswap
base_model: path/to/base_model
models:
  - model: path/to/model
parameters:
  t:
    - value: 0.001
tokenizer_source: path/to/model
dtype: bfloat16