# uv run --no-project --with mergekit merge/nuslerp.yaml --gpu-rich

merge_method: nuslerp
models:
  - model: path/to/model
    parameters:
      weight: 1.4
  - model: path/to/model
    parameters:
      weight: 0.6
tokenizer_source: path/to/model
dtype: bfloat16