# uv run --no-project --with mergekit merge/linear.yaml --gpu-rich

merge_method: linear
models:
  - model: path/to/model
    parameters:
      weight: 0.9
  - model: path/to/model
    parameters:
      weight: 0.1
tokenizer_source: path/to/model
dtype: bfloat16