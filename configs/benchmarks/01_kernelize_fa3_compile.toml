# FA2 + compile + kernelize

[model]
name = "Qwen/Qwen3-8B"
attention = "FA3"
kernelize = true
compile_mode = "max-autotune"

[trainer]
steps = 50
batch_size = 1

[scheduler]
type = "wsd"
warmup_steps = 10
decay_steps = 10

[checkpoint]
save_dir = "benchmark_results/fa3_compile_kernelize"