# Liger + CCE + SDPA + compile

[model]
name = "Qwen/Qwen3-8B"
attention = "SDPA"
liger = true
cce = true
compile_mode = "max-autotune"

[trainer]
steps = 50

[scheduler]
type = "wsd"
warmup_steps = 10
decay_steps = 10

[checkpoint]
save_dir = "benchmark_results/liger_cce_sdpa_compile"